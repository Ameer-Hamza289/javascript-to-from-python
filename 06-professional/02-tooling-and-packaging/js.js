// ============================================================
// 02 — Tooling & Packaging  (JavaScript)
// Read package.example.json + NOTES.md
// ============================================================

console.log(`
JS day-one tooling map
----------------------
Manifest : package.json
Install  : npm i / pnpm i
Lockfile : package-lock.json / pnpm-lock.yaml
Scripts  : npm run <name>
Local bin: npx <tool>
Format   : prettier
Lint     : eslint
Types    : tsc --noEmit

Compare with pyproject.example.toml on the Python side.
`);
